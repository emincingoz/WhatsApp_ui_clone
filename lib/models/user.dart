class User {
  String userName;
  String userImage;
  String lastMessage;
  String lastMessageTime;
  int? statusCount;

  User(this.userName, this.userImage, this.lastMessage, this.lastMessageTime,
      this.statusCount);
}
